# Copy this file to .env and fill in your keys.
# MyCoder reads these at startup via config.yaml's ${VAR} syntax.

# Required: at least one of these must be set depending on which models you use
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-proj-...
